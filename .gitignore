# local python environment
venv/
__pycache__/
*.pyc

# build outputs (regenerated by the respective Makefiles)
cport/build/
demoapp/build/
technical-report/build/
*.o

# editor / OS noise
*.swp
.DS_Store

# files the demo stations receive at runtime (rx_<basename>, rx_broadcast.bin)
demoapp/rx_*

# machine-local Claude Code permissions (absolute paths, not portable)
.claude/settings.local.json

# two-board benchmark scratch (received files, logs, speech source)
results/board_bench_work/
