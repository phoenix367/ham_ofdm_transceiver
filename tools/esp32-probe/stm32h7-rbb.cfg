# STM32H7 through the ESP32 remote_bitbang probe.
#   openocd -f stm32h7-rbb.cfg
# Start rbb_bridge.py first.

adapter driver remote_bitbang
remote_bitbang host localhost
remote_bitbang port 3335

# SWD needs OpenOCD from master; the packaged 0.12 is JTAG-only and will
# say so ("only one transport option; autoselect 'jtag'"). Swap the two
# lines below to use JTAG, which also needs TDI/TDO/PA15/PB3 wired.
transport select swd
# transport select jtag

# Deliberately slow: remote_bitbang has no real clock, but this caps how
# hard OpenOCD pushes. Prove the wiring here, then raise it.
adapter speed 100

source [find target/stm32h7x.cfg]

# The H7 boots with its debug clocks gated in some low-power states;
# connecting under reset avoids losing the DP on a running target.
reset_config srst_only srst_nogate connect_assert_srst
