/* Empty stand-in for the IDF header, so test_pinmask.cpp can compile the
 * sketch on the host. The register macros it needs are defined by the
 * test itself, before it includes the sketch. */
#pragma once
